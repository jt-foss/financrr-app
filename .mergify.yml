pull_request_rules:
  - name: Automatic merge when CI passes, review is approved and label "auto-merge" is present
    conditions:
      - "label=auto-merge"
      - check-success=*
    actions:
      merge:
        method: merge
